{
  "version": "0.2",
  "description": "Bidirectional structural transforms for Claude SDK calls and Codex spawn_agent prose. The tolerant scanner recognizes both single-level object literals and flat named-arg forms; expressions or template strings cause the call to be left intact with a manual-review marker. Calls with no entry here pass through verbatim — useful when the surrounding prose already documents conditional behavior the destination host can interpret on its own.",
  "supported": [
    {
      "id": "agent-spawn",
      "claude_call": "Agent",
      "codex_marker": "ai-config-sync:agent-call",
      "claude_template": "Agent({{ARGS_JSON}})",
      "codex_template": "Use `spawn_agent` with agent_type: \"{{description}}\".\n\nTask:\n{{prompt}}\n",
      "field_aliases": {
        "claude_to_codex": { "subagent_type": "agent_type" },
        "codex_to_claude": { "agent_type": "subagent_type" }
      }
    },
    {
      "id": "team-create",
      "claude_call": "TeamCreate",
      "codex_marker": "ai-config-sync:team-call",
      "claude_template": "TeamCreate({{ARGS_JSON}})",
      "codex_template": "Use `multi_agent_v2.spawn_agent` to launch the \"{{team_name}}\" team. Each member runs in parallel.\n\n{{#each members}}### Member: {{name}} (agent_type: \"{{agent_type}}\", model: \"{{model}}\")\n{{prompt}}\n\n{{/each}}"
    },
    {
      "id": "team-delete",
      "claude_call": "TeamDelete",
      "codex_marker": "ai-config-sync:team-delete-call",
      "claude_template": "TeamDelete({{ARGS_JSON}})",
      "codex_template": "Tear down the \"{{team_name}}\" team (idempotent — skip if already cleaned up)."
    }
  ],
  "unsupported": []
}
